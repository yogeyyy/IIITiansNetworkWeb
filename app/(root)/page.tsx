import TopNavbar from "@/components/TopNavbar";

const Home = async () => {

  return (
    <section className="flex w-full justify-center">
        <TopNavbar />
    </section>
  );
};

export default Home;
